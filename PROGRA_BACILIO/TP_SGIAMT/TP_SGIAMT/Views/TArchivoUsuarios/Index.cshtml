@model IEnumerable<TP_SGIAMT.Models.TArchivoUsuario>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VauArchivo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VauArchivo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PkIauIdArchivo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PkIauIdArchivo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PkIauIdArchivo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
